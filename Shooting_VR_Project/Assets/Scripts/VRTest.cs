using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Valve.VR;

public class VRTest : MonoBehaviour
{
    /*
    private SteamVR_Action_Boolean ActionToHaptic = SteamVR_Actions._default.menu;
    private SteamVR_Action_Vibration _Vibration = SteamVR_Actions._default.Haptic;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        if (ActionToHaptic.GetStateDown(SteamVR_Input_Sources.LeftHand))
        {
            _Vibration.Execute(0, 0.2f, 100, 1f, SteamVR_Input_Sources.LeftHand);
        }
    }
    */
}
